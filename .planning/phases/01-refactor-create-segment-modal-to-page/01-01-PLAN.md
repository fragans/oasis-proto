---
phase: 01-refactor-create-segment-modal-to-page
plan: 01
type: standard
wave: 1
depends_on: []
autonomous: true
files_modified:
  - app/pages/audiences/segments/create.vue
  - app/pages/audiences/segments/index.vue
requirements: []
must_haves:
  - "create.vue page exists at app/pages/audiences/segments/create.vue"
  - "create.vue contains form fields: name, description, type toggle, tags, SegmentRuleBuilder"
  - "create.vue POSTs to /api/segments and navigates to segment detail on success"
  - "index.vue no longer contains UModal or showCreate ref"
  - "index.vue 'New Segment' button navigates to /audiences/segments/create"
---

<objective>
Extract the create-segment modal from segments/index.vue into a dedicated
segments/create.vue page. Follow the same page pattern as campaigns/create.vue
(full-page form with card sections, bottom action bar). Remove modal and related
state from index.vue.
</objective>

<context>
@app/pages/audiences/segments/index.vue
@app/pages/audiences/segments/[id].vue
@app/pages/campaigns/create.vue
@app/components/segment/SegmentRuleBuilder.vue
</context>

<tasks>

<task type="auto">
  <name>Create segments/create.vue page</name>
  <files>app/pages/audiences/segments/create.vue</files>
  <read_first>
    - app/pages/audiences/segments/index.vue (current modal form: lines 208-287, onCreate function: lines 23-41, form reactive: lines 15-20, rules ref: line 21)
    - app/pages/campaigns/create.vue (reference pattern for page-based create form)
    - app/components/segment/SegmentRuleBuilder.vue (component interface)
  </read_first>
  <action>
    Create `app/pages/audiences/segments/create.vue` with:

    1. Script setup:
       - `definePageMeta({ layout: 'default' })`
       - `const router = useRouter()`
       - `const toast = useToast()`
       - `const saving = ref(false)`
       - `const form = reactive({ name: '', description: '', type: 'static' as 'static' | 'dynamic', tags: '' })`
       - `const rules = ref<SegmentRuleGroup | null>(null)`
       - `onCreate` function: POST to `/api/segments` with same body shape as current modal (spread form, split tags by comma, include rules if dynamic). On success: `toast.add({ title: 'Segment created', color: 'success' })`, navigate to `/audiences/segments/${created.id}`

    2. Template — follow campaigns/create.vue card-based layout:
       - `max-w-3xl mx-auto space-y-8` wrapper
       - Header: subtitle "Create a new audience segment"
       - Card section "Segment Details": Name (UInput, required), Description (UInput), Type toggle (two UButtons for static/dynamic same as current modal), conditional SegmentRuleBuilder when type=dynamic
       - Card section "Tags": UInput with comma-separated hint
       - Bottom action bar: Cancel (ghost, navigates to /audiences/segments), Create (primary, :loading="saving", :disabled="!form.name", calls onCreate)

    3. Import type `SegmentRuleGroup` from `~~/shared/types/contact`
  </action>
  <acceptance_criteria>
    - File exists at app/pages/audiences/segments/create.vue
    - File contains `definePageMeta({ layout: 'default' })`
    - File contains `$fetch<Segment>('/api/segments'`
    - File contains `router.push(\`/audiences/segments/`
    - File contains `<SegmentRuleBuilder`
    - File contains `toast.add(`
    - File contains `max-w-3xl mx-auto`
    - File contains `v-if="form.type === 'dynamic'"`
  </acceptance_criteria>
  <verify>File exists and contains all acceptance criteria patterns</verify>
  <done>create.vue page with full form matching current modal functionality</done>
</task>

<task type="auto">
  <name>Remove modal and related state from index.vue</name>
  <files>app/pages/audiences/segments/index.vue</files>
  <read_first>
    - app/pages/audiences/segments/index.vue (full file — identify all modal-related code to remove)
    - app/pages/audiences/segments/create.vue (verify it exists before removing modal)
  </read_first>
  <action>
    Edit `app/pages/audiences/segments/index.vue`:

    1. Remove from script:
       - `showCreate` ref (line 8)
       - `saving` ref (line 11)
       - `form` reactive (lines 15-20)
       - `rules` ref (line 21)
       - `onCreate` function (lines 23-41)
       - `import type ... SegmentRuleGroup` — keep `Segment` import, drop `SegmentRuleGroup` if only used by modal

    2. Remove from template:
       - Entire UModal block (lines 208-287)

    3. Update "New Segment" buttons:
       - Header button (line 69-74): change from `@click="showCreate = true"` to `to="/audiences/segments/create"`
       - Empty state button (line 127-132): change from `@click="showCreate = true"` to `to="/audiences/segments/create"`
  </action>
  <acceptance_criteria>
    - index.vue does NOT contain `UModal`
    - index.vue does NOT contain `showCreate`
    - index.vue does NOT contain `saving`
    - index.vue does NOT contain `onCreate`
    - index.vue contains `to="/audiences/segments/create"` (at least 2 occurrences)
    - index.vue still contains `useSegments()` and segment list rendering
    - index.vue does NOT import `SegmentRuleGroup`
  </acceptance_criteria>
  <verify>grep -c "UModal\|showCreate\|onCreate" index.vue returns 0; grep -c 'to="/audiences/segments/create"' index.vue returns 2</verify>
  <done>index.vue cleaned of all modal code, buttons navigate to create page</done>
</task>

</tasks>

<verification>
- `npx nuxi typecheck` passes (or build succeeds)
- Navigate to /audiences/segments → "New Segment" button links to /audiences/segments/create
- Create page renders form with all fields
- Submitting form creates segment and redirects to detail page
</verification>
