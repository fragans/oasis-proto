services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: oasis
      POSTGRES_USER: oasis
      POSTGRES_PASSWORD: oasis_dev
    volumes:
      - pgdata:/var/lib/postgresql/data


volumes:
  pgdata:
